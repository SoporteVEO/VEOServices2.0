{
  "name": "veo_services_mono",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start": "turbo run start"
  },
  "devDependencies": {
    "shadcn": "^4.7.0",
    "turbo": "^2.5.0"
  },
  "packageManager": "npm@10.2.1",
  "engines": {
    "node": ">=20"
  }
}
